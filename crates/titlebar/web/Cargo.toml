[package]
name = "spiders-titlebar-web"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
spiders-css = { path = "../../css" }
spiders-titlebar-core = { path = "../core" }

[dev-dependencies]
spiders-core = { path = "../../core" }
