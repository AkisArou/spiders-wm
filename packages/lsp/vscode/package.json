{
  "name": "spiders-css-lsp-vscode",
  "displayName": "Spiders CSS",
  "description": "VS Code client for the spiders CSS language server.",
  "version": "0.0.0",
  "publisher": "spiders-wm",
  "repository": {
    "type": "git",
    "url": "https://github.com/AkisArou/spiders-wm.git"
  },
  "main": "./dist/extension.js",
  "types": "./dist/extension.d.ts",
  "license": "MIT",
  "icon": "./media/icon.png",
  "categories": ["Programming Languages"],
  "engines": {
    "vscode": "^1.95.0"
  },
  "activationEvents": [
    "workspaceContains:config.tsx",
    "workspaceContains:config.ts",
    "workspaceContains:config.jsx",
    "workspaceContains:config.js",
    "onCommand:spidersCss.restartServer"
  ],
  "contributes": {
    "commands": [
      {
        "command": "spidersCss.restartServer",
        "title": "Spiders CSS: Restart Language Server"
      }
    ],
    "configuration": {
      "title": "Spiders CSS",
      "properties": {
        "spidersCss.enable": {
          "type": "boolean",
          "default": false,
          "scope": "window",
          "markdownDescription": "Enable the spiders CSS language server for this workspace. This extension stays disabled by default."
        },
        "spidersCss.server.path": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute path to the `spiders-css-lsp` binary. When empty, the extension first tries a bundled platform binary and then common workspace-relative development paths."
        },
        "spidersCss.server.trace": {
          "type": "string",
          "enum": ["off", "messages", "verbose"],
          "default": "off",
          "markdownDescription": "LSP trace level for the spiders CSS language server."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit false",
    "sync:icon": "bash ./scripts/sync-icon.sh",
    "prepare:linux-x64": "bash ./scripts/bundle-linux-x64.sh",
    "bundle:linux-x64": "pnpm run prepare:linux-x64",
    "prepackage": "pnpm run prepare:linux-x64",
    "package": "pnpm run build && vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@types/vscode": "^1.95.0",
    "@vscode/vsce": "^3.6.2",
    "tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vscode-languageclient": "^9.0.1"
  }
}
